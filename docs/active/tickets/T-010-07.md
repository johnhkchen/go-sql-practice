---
id: T-010-07
story: S-010
title: fix-go-build-failures
type: bug
status: done
priority: critical
phase: done
depends_on: [T-010-02]
---

## Context

T-010-01 and T-010-02 left behind unused imports that break `go build` and `go vet`. A test file also has a missing import. The Go backend does not compile.

## Changes

### Remove unused import in `routes/presentations.go`
- Line 4: `"crypto/subtle"` is imported but no longer used — `validateToken()` was moved to `tokens.go` by T-010-02
- Remove the import

### Remove unused import in `routes/links_view.go`
- Line 5: `"strings"` is imported but no longer used — likely left behind when path extraction moved to `extractPathParam()`
- Remove the import

### Fix missing import in `routes/links_view_test.go`
- Line 201: `createTestLinkWithID` takes `*pocketbase.PocketBase` but the file does not import `github.com/pocketbase/pocketbase`
- Add the missing import

## Acceptance Criteria

- `go build .` succeeds
- `go vet ./...` passes with no errors
- `go test ./...` compiles (tests may pass or fail, but must compile)
